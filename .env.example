# Required for live agent calls. Without it every agent falls back to the
# pre-verified cached bank and the flow still completes end to end (P0-16).
ANTHROPIC_API_KEY=sk-ant-...

# Optional. Defaults to claude-opus-5.
PIVOT_MODEL=claude-opus-5
